name: Keep Render Alive

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl to Render Service
        run: |
          curl -s -o /dev/null -w "%{http_code}" https://seed-v1-be.onrender.com/actuator/health
